import { alpha, makeStyles } from '@mui/material/styles';

export default makeStyles((theme) => ({
  search: {
    position: 'relative',
    marginRight:2,
    marginLeft: 0,
    width: '100%',
  },
  
}));

